---
applications:
  - name: zipbank_of_wilmington
    memory: 1G
    instances: 1
    random-route: true
    buildpack: java_buildpack
    path: target/zipBank-0.0.1-SNAPSHOT.jar
    env:
        SPRING_PROFILES_ACTIVE: prod
    services:
     - zipWallet